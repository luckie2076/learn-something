# 要求
1. 请以教学为导向的
    1. 每一个教学科目, 应该放在根目录的一个独立的文件夹下, 相互完全隔离
    2. 根据复杂程度, 将一个科目合理拆分成多个教学单元
    2. 每个教学单元应该循序渐进, 但也要相互完全隔离(包括运行环境), 可单独运行
2. 代码极简, 仅用于演示, 避免引入非必要且复杂的内容
3. 应该基于最新的库或包
4. 应该基于最新的文档和官方的使用教程
5. 请使用中文回答

# 环境 (优先)
* pnpm
* vite
* react + typescript + tailwindcss